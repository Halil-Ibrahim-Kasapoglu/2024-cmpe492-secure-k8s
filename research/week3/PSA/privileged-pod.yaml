apiVersion: v1
kind: Pod
metadata:
  name: privileged-pod
  namespace: privileged-ns
spec:
  containers:
  - name: nginx
    image: nginx
    securityContext:
      privileged: true  # Privileged mode

