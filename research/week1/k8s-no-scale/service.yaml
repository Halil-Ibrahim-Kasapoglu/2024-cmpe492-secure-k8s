apiVersion: v1
kind: Service
metadata:
  name: web-server-apache-service
spec:
  selector:
    app: web-server-apache
  ports:
    - protocol: TCP
      port: 8061         
      targetPort: 8061
      nodePort: 30000
  type: NodePort
